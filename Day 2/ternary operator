#include <stdio.h>

int main()
{   printf("enter==");
    int a,b,c;
    scanf("%d%d%d",&a,&b,&c);
    (a==b&&b==c)?printf("centrio"):(((a+b)>c)?printf("Triangle two"):printf("Triangle not possible"));
    return 0;
    
}
